public class ComputationImplementation implements ComputeEngine {

    private static final String DEFAULT_OUTCOME = "DefaultResult";

    public String compute(int value) {
        return DEFAULT_OUTCOME;
    }
}

